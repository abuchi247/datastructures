# The Flip operator (~) flips every bit in a number
# The output bit of each comparison bit remains the same only if the
# bits to be compared are similar
x = 'A' # 01000001
y = ~ord(x)
print(y) # -66